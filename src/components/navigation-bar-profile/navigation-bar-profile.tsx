import classNames from 'classnames';
import styles from './navigation-bar-profile.module.scss';

export interface NavigationBar_ProfileProps {
    className?: string;
}

/**
 * This component was created using Codux's Default new component template.
 * To create custom component templates, see https://help.codux.com/kb/en/article/kb16522
 */
export const NavigationBar_Profile = ({ className }: NavigationBar_ProfileProps) => {
    return <div><h3>Profile</h3></div>;
};
