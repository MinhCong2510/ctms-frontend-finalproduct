import classNames from 'classnames';
import styles from './navigation-bar-staffs.module.scss';

export interface NavigationBar_StaffsProps {
    className?: string;
}

/**
 * This component was created using Codux's Default new component template.
 * To create custom component templates, see https://help.codux.com/kb/en/article/kb16522
 */
export const NavigationBar_Staffs = ({ className }: NavigationBar_StaffsProps) => {
    return <div><h3>Staffs</h3></div>;
};
